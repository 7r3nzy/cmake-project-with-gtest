dist: trusty
sudo: false
language: cpp

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-7 

before_script:
  - CXX=/usr/bin/g++-7 CC=/usr/bin/gcc-7
  - mkdir build
  - cd build
  - cmake ..
script: make
